apiVersion: apps/v1
kind: Deployment
metadata:
  name: branch-api-main
  namespace: sbs-demo
  labels:
    app: branch-api
    branch: main
spec:
  replicas: 2
  selector:
    matchLabels:
      app: branch-api
      branch: main
  template:
    metadata:
      labels:
        app: branch-api
        branch: main
    spec:
      containers:
        - name: branch-api
          image: branch-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: BRANCH_NAME
              value: "main"
            - name: VERSION
              value: "1.0.0"
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: branch-api-main
  namespace: sbs-demo
  labels:
    app: branch-api
    branch: main
spec:
  selector:
    app: branch-api
    branch: main
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
